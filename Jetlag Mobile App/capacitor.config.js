/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: "com.jetlagmobileapp.app",
  appName: "Jetlag",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

module.exports = config;
