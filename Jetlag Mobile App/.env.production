# Production build config (committed — the domain is not a secret).
# Nginx proxies /ws to the Node sync/push server on localhost:8080.
# VITE_WS_URL is intentionally unset so the app uses the same-origin /jetlag-ws proxy.
